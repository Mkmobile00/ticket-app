<?xml version="1.0" encoding="utf-8"?>
<!--
  Default: cleartext HTTP is BLOCKED (TLS-only) — protects production traffic.
  Cleartext is permitted only to local development hosts so a Wi-Fi/USB device
  can reach the dev Laravel server. Add/replace your PC's LAN IP as needed; in a
  release build these dev hosts simply won't be contacted.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <!-- Dev PC on the LAN (update to match your machine's IP). -->
        <domain includeSubdomains="false">192.168.1.78</domain>
    </domain-config>
</network-security-config>
